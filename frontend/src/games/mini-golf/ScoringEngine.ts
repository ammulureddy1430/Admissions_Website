import type{MiniGolfMetrics}from"./Types";const C=(n:number)=>Math.round(Math.max(0,Math.min(100,n)));export function scoreGolf(x:Record<string,number>,times:number[],shots:number[],duration:number):MiniGolfMetrics{const completion=x.coursesStarted?x.coursesCompleted/x.coursesStarted*100:0,control=x.shotsTaken?x.successfulShots/x.shotsTaken*100:0,collisions=x.shotsTaken?Math.max(0,100-(x.obstacleCollisionCount+x.wallCollisionCount)*6/x.shotsTaken):0,adapt=x.adaptiveAdjustments?x.successfulAdaptiveAdjustments/x.adaptiveAdjustments*100:control;const hand=C(control*.55+completion*.3+collisions*.15),motor=C(completion*.45+adapt*.35+control*.2),visual=C(control*.65+collisions*.35),spatial=C(completion*.55+collisions*.45),force=C(100-(x.overshootCount+x.undershootCount)*8/Math.max(1,x.shotsTaken)),consistency=C(100-(x.powerDeviation||0));const overall=C(hand*.28+motor*.27+visual*.1+spatial*.1+force*.08+adapt*.07+consistency*.1);return{...x,sessionDuration:duration,decisionTimes:times,shotsPerCourse:shots,averageShotsPerCourse:shots.length?shots.reduce((a,b)=>a+b,0)/shots.length:0,averageDecisionTime:times.length?times.reduce((a,b)=>a+b,0)/times.length:0,handEyeCoordinationScore:hand,motorPlanningScore:motor,visualMotorIntegrationScore:visual,spatialJudgmentScore:spatial,precisionScore:control,forceControlScore:force,directionalControlScore:visual,adaptiveMotorControlScore:C(adapt),visualTrackingScore:C(90),responseConsistencyScore:consistency,overallScore:overall,completionStatus:"COMPLETED"}}
