export class SoundManager {constructor(private enabled=true){}setEnabled(v:boolean){this.enabled=v}tone(f:number,d=.08){if(!this.enabled)return;const C=window.AudioContext||((window as unknown as{webkitAudioContext:typeof AudioContext}).webkitAudioContext);if(!C)return;const c=new C(),o=c.createOscillator(),g=c.createGain();o.frequency.value=f;g.gain.setValueAtTime(.035,c.currentTime);g.gain.exponentialRampToValueAtTime(.001,c.currentTime+d);o.connect(g);g.connect(c.destination);o.start();o.stop(c.currentTime+d)}move(){this.tone(145)}park(){this.tone(260)}exit(){this.tone(440,.18)}}
