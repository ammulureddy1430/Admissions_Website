export class TrainSoundManager{private context?:AudioContext;constructor(private enabled:boolean){}setEnabled(v:boolean){this.enabled=v}play(frequency=330,duration=.16){if(!this.enabled)return;this.context||=new AudioContext();const o=this.context.createOscillator(),g=this.context.createGain();o.frequency.value=frequency;g.gain.setValueAtTime(.045,this.context.currentTime);g.gain.exponentialRampToValueAtTime(.001,this.context.currentTime+duration);o.connect(g).connect(this.context.destination);o.start();o.stop(this.context.currentTime+duration)}dispose(){void this.context?.close()}}
