import type{Direction,PipeKind,PipePiece,PipelineLayout}from"./Types";
const DIR:Record<string,Direction>={"0,-1":"N","1,0":"E","0,1":"S","-1,0":"W"};
const ORDER:Direction[]=["N","E","S","W"];
const paths=[[[0,1],[1,1],[2,1],[3,1]],[[0,1],[1,1],[1,2],[2,2],[3,2]],[[0,1],[1,1],[1,2],[2,2],[2,1],[3,1]],[[0,2],[1,2],[1,1],[2,1],[2,2],[3,2],[4,2]]] as number[][][];
function rotationFor(open:Direction[]){if(open.length===2&&((open.includes("N")&&open.includes("S"))||(open.includes("E")&&open.includes("W"))))return(open.includes("N")?0:1)as 0|1;const base:Direction[]=["N","E"];for(let r=0;r<4;r++)if(base.map(d=>ORDER[(ORDER.indexOf(d)+r)%4]).every(d=>open.includes(d)))return r as 0|1|2|3;return 0}
export class PipelineGenerator{generate(level:number):PipelineLayout{const l=Math.max(1,Math.min(4,level)),coords=paths[l-1],pieces:PipePiece[]=coords.map((cell,i)=>{const previous=coords[i-1],next=coords[i+1],open:Direction[]=[];if(previous)open.push(DIR[`${previous[0]-cell[0]},${previous[1]-cell[1]}`]);else open.push("W");if(next)open.push(DIR[`${next[0]-cell[0]},${next[1]-cell[1]}`]);else open.push("E");const kind:PipeKind=open[0]===({N:"S",S:"N",E:"W",W:"E"}as Record<Direction,Direction>)[open[1]]?"straight":"corner",solutionRotation=rotationFor(open),offset=((i+l)%3)+1;return{id:`pipe-${i}`,x:cell[0],y:cell[1],kind,solutionRotation,rotation:((solutionRotation+offset)%4)as 0|1|2|3}});return{level:l,cols:l>=4?5:4,rows:l>=4?5:4,pieces,path:pieces.map(p=>p.id),source:{x:coords[0][0],y:coords[0][1]},destination:{x:coords.at(-1)![0],y:coords.at(-1)![1]},optimalRotations:pieces.reduce((sum,p)=>sum+(4+p.solutionRotation-p.rotation)%4,0)}}}
