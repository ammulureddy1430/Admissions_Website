export class SoundManager{constructor(private enabled=true){}play(type:"rotate"|"flow"|"finish"){if(!this.enabled||typeof window==="undefined")return;const context=new AudioContext(),osc=context.createOscillator(),gain=context.createGain();osc.frequency.value=type==="rotate"?280:type==="flow"?440:620;gain.gain.setValueAtTime(.025,context.currentTime);gain.gain.exponentialRampToValueAtTime(.001,context.currentTime+.16);osc.connect(gain).connect(context.destination);osc.start();osc.stop(context.currentTime+.17)}}
